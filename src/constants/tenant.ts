/** Internal Prisma data scope shared with seed data. Not an OXID claim and not shown in the UI. */
export const DATA_TENANT_ID = "demo-tenant";

/** OXID OAuth users are standard Users until roles come from the shop. */
export const OXID_SESSION_ROLE = "user" as const;

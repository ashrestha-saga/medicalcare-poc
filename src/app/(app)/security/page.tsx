import { SecurityScreen } from "@/components/features/security/SecurityScreen";

/** /security — TOTP enrollment for any signed-in user (account:security). */
export default function SecurityPage() {
  return <SecurityScreen />;
}

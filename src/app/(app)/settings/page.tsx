import { SettingsScreen } from "@/components/features/settings/SettingsScreen";

/** /settings — clinic settings (settings:view). */
export default function SettingsPage() {
  return <SettingsScreen />;
}

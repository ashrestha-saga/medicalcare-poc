import { DevicesScreen } from "@/components/features/devices/DevicesScreen";

/** /devices — tenant device inventory (inventory:view). */
export default function DevicesPage() {
  return <DevicesScreen />;
}
